# Must match backend JWT_PUBLIC_KEY (base64-encoded PEM public key)
EXPO_PUBLIC_JWT_PUBLIC_KEY=

# Backend API reachable from your phone (use LAN IP, not localhost)
EXPO_PUBLIC_API_URL=http://192.168.x.x:3000/api/v1
